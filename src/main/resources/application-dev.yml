spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_sales_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Anh271020k5

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.carsales: DEBUG
    org.springframework.web: DEBUG
